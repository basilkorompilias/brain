{
  "mcpServers": {
    "brand-brain": {
      "command": "${workspaceFolder}/.venv/Scripts/python.exe",
      "args": [
        "-m",
        "brand_brain.server"
      ],
      "cwd": "${workspaceFolder}"
    }
  }
}
